// Static deployment entry point is now [index.html].
// This original JSX component required a React build pipeline and UI dependencies,
// so the app was rewritten as a self-contained SPA for direct hosting.
